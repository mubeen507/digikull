import { TailSpin } from 'react-loader-spinner'

const Loader = () => (
    <TailSpin
                height={80}
                width={80}
                radius={9}
                color="#F37D3C"
            />
)

export default Loader